# AtomUICityApplication

Generated by `dotnet new atomui-city-app`.

The generated project is a minimal AtomUI.City Avalonia desktop application. `Program` owns the City Host lifecycle, while `App` attaches the Presentation runtime and resolves the main window from dependency injection.
